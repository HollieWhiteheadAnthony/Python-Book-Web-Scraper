# Data

Running `python scraper.py` creates:

`books_cleaned.csv`

The output contains one row per book with cleaned price, rating, availability, and stock fields.

The source website is Books to Scrape, a site intentionally provided for web-scraping practice.
